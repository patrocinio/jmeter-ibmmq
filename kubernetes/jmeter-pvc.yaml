apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jmeter-pvc2
  finalizer: null
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 256M
  storageClassName: ocs-storagecluster-cephfs